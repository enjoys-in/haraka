# Dependencies (reinstalled inside the image)
node_modules
**/node_modules

# Build output (rebuilt inside the image)
admin/web/dist
admin/server/dist

# Runtime artifacts
*.log
queue/*
!queue/.gitkeep
spool/*
!spool/.gitkeep

# Docker / git / editor noise
.git
.gitignore
.dockerignore
Dockerfile
docker-compose*.yml
.vscode
.idea
.DS_Store

# Local-only examples (not needed in the runtime image)
example
docs