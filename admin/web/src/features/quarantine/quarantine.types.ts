// Quarantine shapes — messages held on disk by the quarantine plugin. The
// canonical definitions live in lib/types so the API client and UI stay in sync.
export type { QuarantineReason, QuarantinedMessage } from '@/lib/types';
