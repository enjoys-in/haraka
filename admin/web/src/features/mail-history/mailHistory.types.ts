// Mail history / delivery log shapes. The canonical definitions live in
// lib/types so the API client and the UI stay in sync.
export type { MailDirection, DeliveryStatus, MailRecord } from '@/lib/types';
