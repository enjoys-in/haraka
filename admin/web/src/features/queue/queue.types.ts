// Outbound mail queue shapes (Haraka outbound / deferred spool). The canonical
// definitions live in lib/types so the API client and the UI stay in sync.
export type { QueueState, QueuedMessage, QueueSummary } from '@/lib/types';
