// Core server settings shapes (config/me, connection.ini, smtp.ini). The
// canonical definition lives in lib/types so the API client and UI stay in sync.
export type { ServerSettings } from '@/lib/types';
